# pnpm build-script approval. Sharp's install/check.js falls through to a
# source build when a system libvips is present; skip the script and rely on
# the prebuilt @img/sharp-* platform binary at runtime.
allowBuilds:
  esbuild: true
  sharp: false
